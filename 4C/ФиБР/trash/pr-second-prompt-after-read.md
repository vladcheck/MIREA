Тебе нужно

- реализовать задачи из подраздела "### Практическое задание"
- создать скрипт run.sh (#!/bin/zsh) в корне проекта, для запуска сервера, генерации swagger и запуска документации
- создать и настроить файл swagger.js в корне проекта
- создать .env.example в корне проекта. Секреты оставь пустыми (например `PASSWORD=` ), остальные поля заполни самостоятельно

Технические требования:

- я использую пакетный менеджер pnpm@11.0.8. погугли официальную доку про v11
- в коде ЗАПРЕЩЕНЫ комментарии js-doc именно для swagger
- Автоматически генерируй документацию при помощи "swagger-ui-express": "^5.0.0" и "swagger-autogen": "^2.23.7"
- Создание базы данных и поднятие схемы должно происходить бесшовно непосредственно в файлах js, zsh или/и sql. Предпочтительно создать файл js

Требования к выводу:

- Если ты создаешь новый файл, указывай, как он называется и по какому пути он лежит. Путь относительно корня проекта, считай как `/`
- Если ты в дальнейшем будешь предлагать изменения в файле, пиши путь к файлу относительно корня проекта, и только те строки, которые ты меняешь, а не весь файл
